/**
 * NavbarCommonSharedComponent.tsx
 *
 * DEPRECATED — the shared navigation is now handled by SiteHeader
 * (src/components/ui/site-header.tsx) which is rendered in the root
 * layout for ALL pages including the homepage.
 *
 * This file is kept to avoid breaking any imports, but renders nothing.
 */
export default function NavbarCommonSharedComponent() {
  return null;
}
